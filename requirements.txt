# Mac (M5 Pro) ortamı — CoreML/ANE ölçüm ayağı
# Python 3.12 (uv venv). Sürümler kurulumdan sonra `uv pip freeze` ile lock'lanacak.
coremltools
einops==0.8.2
fvcore==0.1.5.post20221221
numpy
onnx
onnxruntime
pillow
psutil
timm
timm==1.0.28
torch
torchvision
